{
  "name": "@rewind/protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The single source of truth shared by the Rust daemon, the extensions and the UI: event schemas, the redaction pattern registry, and versioned prompts.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./redaction/patterns.json": "./redaction/patterns.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@rewind/config": "workspace:*",
    "@rewind/fixtures": "workspace:*"
  }
}
