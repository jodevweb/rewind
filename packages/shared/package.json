{
  "name": "@rewind/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Utilities shared by the UI and the extensions.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@rewind/config": "workspace:*"
  },
  "dependencies": {
    "@rewind/fixtures": "workspace:*"
  }
}
