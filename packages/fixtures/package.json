{
  "name": "@rewind/fixtures",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Golden sessions, the redaction corpus and the search evaluation set. Shared by the Rust and TypeScript test suites.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsx src/build.ts",
    "build:check": "tsx src/build.ts --check",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@rewind/config": "workspace:*",
    "tsx": "^4.19.2"
  }
}
