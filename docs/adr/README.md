# Architecture Decision Records

One file per decision that deviates from, or materially extends, the design documents in `docs/`.

Numbered sequentially. Copy `0000-template.md`. Keep them short — the value is in the
_alternatives considered_ and the _consequences_, not in the narrative.
