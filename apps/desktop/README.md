# @rewind/desktop

Tauri v2 desktop application: React frontend + src-tauri Rust workspace (Phase 1).

Not implemented yet. See `docs/ROADMAP.md` for the tickets that build this, and
`docs/ARCHITECTURE.md` for how it fits into the system.
